name: web-perplexity
version: 1.0.0
description: "Perplexity Search API web search backend. Uses the dedicated /search endpoint (raw ranked results, no LLM answer). Requires PERPLEXITY_API_KEY."
author: bo17age (migrated to /search endpoint)
kind: backend
provides_web_providers:
  - perplexity
